# student-seating

A frontend-only Javascript application to manage the seating plan of a classroom.

Wire up your own backend to fetch/retrieve the actual students' data.

Demo php backend (using SQLite as DB) included in `api` folder, but not recommended for production use.

# Dependencies

Good old bootstrap (version 4) and jquery (3.5.1).